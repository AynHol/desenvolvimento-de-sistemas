import read from "readline-sync";

export default function exercicio40() {
    var menor: number[] = []
    var maior : number[] = []

    for(var i = 0;i<30;i++){
        var temperatura = Math.floor(Math.random() * 35);
    }
}